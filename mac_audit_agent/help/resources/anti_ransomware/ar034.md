# Containment Authorization Expired

The bounded decision or lease authorization expired before action. MSAA sent no signal. Refresh the incident and obtain a new authenticated, identity-bound authorization; never replay an old nonce, token, or pre-reboot request.
