# Anti-Ransomware Protocol Version Is Incompatible

The communicating sensor, engine, notifier, or helper uses an incompatible protocol version. Install components from the same release, verify their build identifiers, and retry. Protocol downgrade is intentionally rejected.
