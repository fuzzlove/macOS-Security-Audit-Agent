# Required MSAA Dependency Is Missing

A dependency required by the selected feature is unavailable in the active runtime. Confirm the interpreter path and install the pinned dependency group documented for that feature. Frozen applications must be rebuilt; do not attempt pip repair inside an application bundle.
