# Python Runtime Is Not Supported

The selected interpreter is outside the runtime policy for this MSAA operation. Source, test, GUI, and packaging support are separate. Use the runtime named by the diagnostic and do not assume the newest installed Python is a validated release toolchain. For packaging, use Python 3.12.x unless running the explicitly experimental compatibility workflow.
