# Recovery Impairment Detection

The safe command-intent classifier detects Time Machine/APFS snapshot deletion patterns and MSAA service impairment without executing commands. Production correlation will combine actor identity, parent, signing identity, maintenance window, target, and incident state. Approved maintenance lowers but does not erase risk. Real snapshots and backup services are never altered by tests.
