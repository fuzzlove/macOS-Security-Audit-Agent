# Recommended end-user installation: desktop UI plus all graceful-enhancement
# features.  Platform/Python markers in pyproject.toml automatically skip
# components that cannot run on the selected interpreter.
-e .[compat,desktop,crypto,exports,network]

# For a minimal CLI/doctor-only host, use requirements-core.txt instead.
